<% content_for :title, "New travel" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Nova viagem</h1>

  <%= render "form", travel: @travel %>

  <%= link_to "voltar", travels_path %>
</div>
